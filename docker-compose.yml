services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      RESUME_DIR: /app/data/resumes
      JOBS_DB_PATH: /app/storage/jobs.db
    volumes:
      - ${RESUME_HOST_DIR:-./data/resumes}:/app/data/resumes:ro
      - ${DATA_DIR:-./storage}:/app/storage
