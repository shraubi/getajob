# Legacy LLM/RAG implementation

This directory is a frozen reference snapshot of the former LiteLLM, agent, RAG, and PDF-generation approach.

It is intentionally outside the production package and is not imported, installed, tested, copied into the Docker image, or deployed. New work belongs in `jobbot/`. Remove this archive later when its historical value expires.
